let kiemtra = prompt("Mời bạn nhập vào một chuỗi ký tự");
 if (kiemtra.includes(" ")) {  
     document.write("chuỗi chứa dấu cách");  
    } else {  
        document.write("chuỗi không chứa dấu cách");  
    }  
